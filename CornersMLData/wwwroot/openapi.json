{
  "openapi": "3.0.1",
  "info": {
    "title": "CornersMLData API",
    "version": "v1",
    "description": "Minimal OpenAPI document for CornersMLData endpoints (generated statically)."
  },
  "servers": [
    { "url": "/" }
  ],
  "paths": {
    "/api/CornersDataScrapping/scrape-lineups": {
      "get": {
        "summary": "Scrape lineups",
        "operationId": "ScrapeLineups",
        "parameters": [
          {
            "name": "cancellationToken",
            "in": "query",
            "schema": { "type": "string" },
            "required": false,
            "description": "Cancellation token (ignored when calling via browser)."
          }
        ],
        "responses": {
          "200": {
            "description": "List of processed rows",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": { "type": "object" }
                }
              }
            }
          },
          "500": { "description": "Server error" }
        }
      }
    }
  }
}
